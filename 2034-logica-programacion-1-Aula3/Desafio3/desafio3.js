// Hemos llegado a otra lista de actividades (no obligatorias) para que practiques y refuerces aún más tu aprendizaje. ¿Vamos a hacerlo? Desafíos:

// Crea un contador que comience en 1 y vaya hasta 10 usando un bucle 'while'. Muestra cada número.
let contador = 1;

while (contador <= 10) {
    console.log(contador)
    contador++
}
// Crea un contador que comience en 10 y vaya hasta 0 usando un bucle 'while'. Muestra cada número.
let c = 10;

while (c >= 0) {
    console.log(c)
    c--
}

// Crea un programa de cuenta regresiva. Pide un número y cuenta desde el numero hasta cero utilizando un bucle 'while' en la consola del navegador.
let n = parseInt(prompt("coloca un numero positivo"));


while (n >= 0) {
    console.log(n)
    n--
}

// Crea un programa de cuenta progresiva. Pide un número y cuenta desde 0 hasta ese número utilizando un bucle 'while' en la consola del navegador.
let m = parseInt(prompt("coloca un numero positivo"));
let j = 0;

while ( m >= j) {
    console.log(j)
    j++
}
